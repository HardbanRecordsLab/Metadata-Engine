[pytest]
asyncio_mode = auto
# plain `pytest` (as run in CI) does not put backend/ on sys.path, so `import app` failed
pythonpath = .
testpaths = tests app/routes
python_files = test_*.py
python_classes = Test*
python_functions = test_*
