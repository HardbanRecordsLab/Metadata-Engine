# DEPRECATED (2026-09-21) - do not use directly. Production runs from docker-compose.yml
# (Postgres hbrl-postgres / metadata_engine, over the external hbrl-db network).
#
# This file used to pin DATABASE_URL=sqlite:////data/music_metadata.db and mount ./.env. That pin
# silently switched production back to an empty SQLite file when the "Deploy to VPS" workflow
# ran on 2026-09-21. It is kept only as an alias so old commands and links keep working
# without flipping the database; delete it once nothing references it.
include:
  - docker-compose.yml
