{
  "name": "music-metadata-engine-unified",
  "version": "3.0.0",
  "description": "Unified Music Metadata Engine with Frontend and Backend",
  "scripts": {
    "start": "concurrently \"npm run start:frontend\" \"npm run start:backend\"",
    "start:frontend": "cd frontend && npm run dev",
    "start:backend": "cd backend && ..\\venv\\Scripts\\python.exe -m uvicorn app.main:app --reload --port 8080",
    "install:all": "npm install && cd frontend && npm install && cd backend && pip install -r requirements.txt",
    "build:frontend": "cd frontend && npm run build"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}